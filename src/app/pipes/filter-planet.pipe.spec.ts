import { FilterPlanetPipe } from './filter-planet.pipe';

describe('FilterPlanetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlanetPipe();
    expect(pipe).toBeTruthy();
  });
});
